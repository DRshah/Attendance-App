<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.attendanceapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddStudentActivity" />
        <activity android:name=".AddTeacherActivity" />
        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".AttendanceActivity" />
        <activity android:name=".TeacherActivity" />
        <activity android:name=".SelectAttendanceActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".Edit_activity" />
        <activity android:name=".TeacherLoginActivity" />
        <activity android:name=".CheckAttendanceActivity"></activity>
    </application>

</manifest>
